{
  "name": "linka",
  "version": "0.3.1",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -n client,server -c cyan,green \"npm run dev -w apps/client\" \"npm run dev -w apps/server\"",
    "build": "npm run build -w apps/client && npm run build -w apps/server",
    "start": "npm run start -w apps/server",
    "check": "npm run check -w apps/client && npm run check -w apps/server"
  },
  "devDependencies": {
    "concurrently": "^9.2.0",
    "typescript": "^5.8.3"
  }
}
